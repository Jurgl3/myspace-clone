comments:
  allowedFields: ["name", "message"]
  requiredFields: ["name", "message"]
  branch: "main"
  path: "comments"
  filename: "comment-{@timestamp}"
  format: "json"
  moderation: true
  name: "Guestbook Comments"
  commitMessage: "New comment submitted"
  generatedFields:
    date:
      type: date
      options:
        format: iso8601
